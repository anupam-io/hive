HAMMER $HAMMER_N of $HAMMER_MAX (continuation). Same issue (#$ISSUE_NUMBER).
Are all the code changes needed for this issue actually present in the
working tree right now? If not, finish them. The harness will commit,
push, open/merge the PR after you exit — do NOT run git or gh yourself.
If you're blocked, output specific questions for a human (the harness
will post them as a GitHub PR comment) and end with exactly
'RESULT: NEEDS_HUMAN'. Otherwise end with 'RESULT: SUCCEEDED'.
