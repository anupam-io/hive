<!-- hive pr-needs-human template — rendered by pr_needs_human_comment -->

## Needs human input — issue #${ISSUE_NUMBER}

**Agent** `${CHATID}` · **role** `${ROLE}` · **task_type** `${TASK_TYPE}` · **hammers** ${HAMMERS_USED}/${HAMMER_MAX}

### Why

${NEEDS_HUMAN_REASON}

### Open questions

${OPEN_QUESTIONS_BLOCK}

### What to do next

${NEXT_STEP_HINT}
