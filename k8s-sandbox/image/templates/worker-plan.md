PLAN (AUTHORITATIVE SCOPE). The issue body contains a `## Plan` section
written or approved by the driver+human. Treat it as the authoritative
scope of this fire — implement exactly what the Plan describes, no more.
If you discover the Plan is wrong, ambiguous, or missing a critical
constraint, STOP with RESULT: NEEDS_HUMAN and ask in the `**Open
questions:**` block. Do NOT silently expand scope. The in-pod review will
flag any out-of-Plan changes as issues.
